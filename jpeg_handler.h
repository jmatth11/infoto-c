#include <stdbool.h>

/**
 * Read JPEG image
 *
 * @param file_name The file name of the JPEG file.
 * @returns bool  True if everything was successful, false otherwise
 */
bool read_img(const char *file_name);

